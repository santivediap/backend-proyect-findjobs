h3 Your offers published
    each object in Offers
        article(class="article_offer")
            div(class="dataOffer")
                each valor, key in object
                    if key == "error"
                        h3 ERROR! #{valor}

                    if key == "title"
                        h3 #{valor}

                    if key == "companyName"
                        h3 #{valor}

                    if key == "location"
                        div(class="dataOfferItem") 
                            p Ubicación: 
                            p #{valor}
                    
                    if key == "experience"
                        div(class="dataOfferItem")
                        p Experiencia: 
                        p #{valor} 
                    
                    if key == "contract_type"
                        div(class="dataOfferItem")
                        p Tipo de contrato: 
                        p #{valor}

                    if key == "work_schedule"
                        div(class="dataOfferItem")
                            p Jornada: 
                            p #{valor}
                    
                    if key == "salary"
                        div(class="dataOfferItem")
                            p Salario: 
                            p #{valor}

                    if key == "description"
                        p Descripción: #{valor}

                    if key == "_id"
                        button(class="remoffer" id=valor) Remove offer
               
